<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.currentplacedetailsonmap">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <!-- The API key for Google Maps-based APIs. -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCpzEjXT-Mh1J7-oZhfcEx2xoEYuusxA-o" />

        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_maps">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".unitActivity" />
        <activity android:name=".ZoneActivity" />
        <activity android:name=".transActivity" />
        <activity android:name=".ryzeActivity" />
        <activity android:name=".factoryActivity" />
        <activity android:name=".shredActivity" />
        <activity android:name=".txActivity" />
        <activity android:name=".allActivity" />
        <activity android:name=".parkActivity" />
        <activity android:name=".streetActivity" />
        <activity android:name=".beginnerActivity"></activity>
    </application>

</manifest>